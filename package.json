{
  "name": "geometry-os",
  "version": "1.0.0",
  "private": true,
  "description": "Geometry OS certified question pipeline (AI generates, the pipeline certifies).",
  "type": "module",
  "dependencies": {
    "@resvg/resvg-js": "^2.6.2"
  }
}
